# One API Hub 通用站点扩展机制实施计划

## 一、项目目标

设计并实现一个**通用的站点类型扩展机制**,使项目能够支持任意类型的 API 站点(不仅限于 one-api 系列),包括:
- OpenAI 官方 API
- Claude API (Anthropic)
- 其他自定义 API 系统
- 任何有余额/使用量概念的 API 服务

## 二、核心设计理念

### 2.1 架构模式
- **适配器模式 (Adapter Pattern)**: 每个站点类型是一个独立的适配器
- **策略模式 (Strategy Pattern)**: 通过统一接口实现不同的 API 调用策略
- **注册中心模式**: 集中管理所有适配器的注册和查找

### 2.2 设计原则
1. **可扩展**: 新增站点类型不改核心业务逻辑（新增适配器 + 注册即可）
2. **统一契约**: 接口 + 能力枚举（capabilities）驱动所有行为
3. **配置驱动**: 通过配置定义站点差异（请求头、字段映射、端点差异等）
4. **向后兼容**: 现有 one-api 账号/功能无缝迁移
5. **渐进式实现**: 允许适配器只实现部分能力（未声明能力不得被调用）

### 2.3 范围与非目标（先写清楚，避免落地分歧）
- **P0 范围**: 内置适配器（编译期引入）+ 注册中心统一路由；UI/存储仍以“余额/今日用量”为核心展示
- **P0 约束**: “站点类型识别”和“账号自动识别（需要登录态）”分开设计，避免概念混淆
- **非目标（暂不做）**: 运行时热加载第三方脚本/适配器市场（涉及安全与签名/更新体系），可在后续阶段推进

## 三、架构设计

### 3.1 整体架构

```
UI Layer (React)
    ↓
Service Layer (AccountManager)
    ↓
Adapter Registry (适配器注册中心)
    ↓
┌─────────────┬─────────────┬─────────────┐
│ OneApiAdapter│ OpenAiAdapter│ ClaudeAdapter│
└─────────────┴─────────────┴─────────────┘
         ↓
  ISiteAdapter (统一接口)
```

### 3.2 核心组件

| 组件 | 职责 | 文件位置 |
|------|------|---------|
| **ISiteAdapter** | 定义适配器统一接口 | `adapters/ISiteAdapter.ts` |
| **SiteAdapterRegistry** | 适配器注册和查找 | `adapters/SiteAdapterRegistry.ts` |
| **OneApiAdapter** | OneAPI 系列适配器实现 | `adapters/OneApiAdapter.ts` |
| **CubenceAdapter** | Cubence 站点适配器实现（Cookie 登录态） | `adapters/CubenceAdapter.ts` |
| **AccountManager** | 账号管理核心服务 | `services/AccountManager.ts` |
| （可选）AdapterFactory | 适配器工厂 | 未实现（当前由 `SiteAdapterRegistry` 直接注册内置适配器） |

## 四、实施步骤

### 阶段 1: 基础架构搭建 (核心接口和注册中心)

#### 1.1 创建适配器接口定义
**文件**: `adapters/ISiteAdapter.ts`

**核心接口（能力驱动，避免“接口必需方法”与“可选功能”矛盾）**:
```typescript
export interface ISiteAdapter {
  // 元数据
  readonly metadata: SiteAdapterMetadata;

  // 基础：必须实现
  validateConnection(credentials: SiteCredentials): Promise<ValidateResult>;

  // 下列方法为可选能力：只有在 metadata.capabilities 声明后才能调用
  getAccountBalance?(credentials: SiteCredentials): Promise<BalanceInfo>;
  getUsageStats?(credentials: SiteCredentials, timeRange: TimeRange): Promise<UsageStats>;

  // 账号自动识别：需要浏览器登录态/本地存储/Cookie 等，仅部分站点支持
  autoDetectAccount?(siteUrl: string): Promise<AutoDetectResult>;

  // one-api 系列：如果 access_token 缺失，尝试用 cookie 创建
  getOrCreateAccessToken?(credentials: SiteCredentials): Promise<AccessTokenInfo>;

  // Token 管理（可选）
  getApiTokens?(credentials: SiteCredentials, pagination?: PaginationParams): Promise<ApiToken[]>;
  createApiToken?(credentials: SiteCredentials, tokenConfig: TokenConfig): Promise<boolean>;
  updateApiToken?(credentials: SiteCredentials, tokenId: string, tokenConfig: TokenConfig): Promise<boolean>;
  deleteApiToken?(credentials: SiteCredentials, tokenId: string): Promise<boolean>;

  // 模型能力（可选）
  getAvailableModels?(credentials: SiteCredentials): Promise<ModelInfo[]>;
  getModelPricing?(credentials: SiteCredentials): Promise<PricingInfo>;

  // 站点状态/公告等（可选）
  getSiteStatus?(siteUrl: string): Promise<SiteStatusInfo>;
}
```

**关键类型（建议补齐定义，保证文档自洽）**:
- `SiteAdapterMetadata`: 适配器元数据（id、name、version、supportedSiteTypes、capabilities，以及可选的 balance 元信息等）
- `AdapterCapability`: 能力枚举（自动检测、余额查询、用量统计、令牌管理、模型列表、模型定价等）
- `SiteCredentials`: 认证凭据（推荐使用 `auth` 联合类型，而不是“堆字段”）
- `ValidateResult`: 连接校验结果（`ok` + `message` + 可选 `details`）
- `BalanceInfo`: 标准化余额信息（`rawBalance/rawUnit` + 可选 `balanceUSD/conversionFactor`）
- `UsageStats`: 标准化用量信息（`rawConsumption/rawUnit` + 可选 `conversionFactor/tokens/requests`）
- `ApiToken`: 标准化令牌信息

**方法约定（落地时建议写进注释/单测）**:
1. `metadata.capabilities` 是唯一可信开关：`AccountManager/UI` 必须先检查能力再调用对应方法
2. `SiteAdapterRegistry.registerAdapter()` 在注册时做一致性校验：声明了能力但方法未实现 → 直接拒绝注册（fail fast）
3. `conversionFactor` 语义统一为“rawUnit 每 1 USD 的数量”（one-api: 500000），避免 UI/服务层各自写死换算
4. 对“不支持”的能力统一抛 `NotSupportedError`（或不实现方法），不要返回空对象造成后续空指针/脏数据

**建议的类型骨架（示例）**:
```typescript
export type SiteAuth =
  | { kind: "one-api-token"; userId: number; accessToken: string }
  | { kind: "api-key"; apiKey: string }
  | { kind: "cookie"; userId?: number };

export interface SiteCredentials {
  siteUrl: string;
  auth: SiteAuth;
  adapterConfig?: Record<string, any>;
}

export interface ValidateResult {
  ok: boolean;
  message?: string;
  details?: any;
}

export interface BalanceInfo {
  rawBalance: number;
  rawUnit: string;
  conversionFactor?: number;
  balanceUSD?: number;
}

export interface UsageStats {
  rawConsumption: number;
  rawUnit: string;
  conversionFactor?: number;
  promptTokens?: number;
  completionTokens?: number;
  requestCount?: number;
}
```

#### 1.2 创建适配器注册中心
**文件**: `adapters/SiteAdapterRegistry.ts`

**核心功能**:
```typescript
export class SiteAdapterRegistry {
  // 单例模式
  private static instance: SiteAdapterRegistry;

  // 注册适配器（注册时做能力/方法一致性校验）
  registerAdapter(adapter: ISiteAdapter): void;

  // 获取适配器（site_type 可为别名：如 new-api/veloera -> one-api）
  getAdapter(siteType: string): ISiteAdapter | null;

  // 获取所有适配器（去重）
  getAllAdapters(): ISiteAdapter[];

  // 获取所有可用 site_type（用于 UI 下拉列表）
  getSupportedSiteTypes(): string[];

  // 站点类型识别（无需登录态；尽量用轻量 probe/特征字段）
  detectSiteType(siteUrl: string): Promise<string | null>;

  // 检查是否支持某个站点类型
  isSiteTypeSupported(siteType: string): boolean;

  // （建议）在初始化阶段注册内置适配器
  // private registerBuiltInAdapters(): void;
}
```

#### 1.3 更新数据类型定义
**文件**: `types/index.ts`

**新增字段**:
```typescript
export interface SiteAccount {
  // ... 现有字段
  site_type: string;  // 新增: 站点类型标识（用于路由到适配器）
  adapter_config?: Record<string, any>;  // 新增: 适配器特定配置（可选）
}

export interface AccountInfo {
  // 账号标识：one-api 为 userId；其他站点可能没有或为字符串
  id?: number | string;

  // 鉴权：不同站点使用不同字段（至少二选一）
  access_token?: string;  // one-api/new-api 等
  api_key?: string;       // OpenAI/Claude 等

  username?: string;

  // 兼容当前 UI 的展示字段（建议入库存储前补齐默认值 0）
  quota: number;
  today_prompt_tokens: number;
  today_completion_tokens: number;
  today_quota_consumption: number;
  today_requests_count: number;

  extra?: Record<string, any>;  // 适配器额外信息（可选）
}
```

#### 1.4 Cookie 登录态站点的请求策略（MV3 重点）
很多站点需要依赖浏览器登录态（Cookie / HttpOnly Cookie）访问用户中心接口。此时需要注意：

1. **扩展侧 `fetch(..., { credentials: "include" })` 不一定会携带 Cookie**：若站点 Cookie 为 `SameSite=Lax/Strict`，从扩展 origin 发起请求通常会被视为 third-party 请求，Cookie 可能不会被附带。
2. **无法在 `fetch` 里手动设置 `Cookie` 请求头**（浏览器禁止），且 HttpOnly Cookie 也无法通过 JS 读取。

因此建议采用“页面上下文请求”的稳定方案：

- 在 `background.ts` 中临时打开目标站点窗口（first-party 上下文）
- 用 `chrome.scripting.executeScript({ world: "MAIN" })` 在页面上下文执行 `fetch(fetchUrl, { credentials: "include" })`
- 将 JSON 结果回传给扩展，再关闭临时窗口

项目落地做法（已实现）：
- `background.ts` 新增消息动作：`pageFetchJson`
  - 请求：`{ action: "pageFetchJson", url, fetchUrl, requestId }`
  - 响应：`{ success: boolean, data?: any, error?: string }`
- `package.json#manifest.permissions` 增加 `scripting`
- 适配器侧可以先尝试普通 `fetch(credentials: "include")`，失败（如 401）后再回退到 `pageFetchJson`（如 `CubenceAdapter`）

---

### 阶段 2: OneAPI 适配器实现 (迁移现有逻辑)

#### 2.1 创建 OneApiAdapter
**文件**: `adapters/OneApiAdapter.ts`

**实现要点**:
1. **元数据定义**:
   ```typescript
   readonly metadata: SiteAdapterMetadata = {
     id: "one-api",
     name: "OneAPI 系列",
     version: "1.0.0",
     supportedSiteTypes: ["one-api", "new-api", "veloera", "one-hub", "done-hub"],
     capabilities: [
       AdapterCapability.AUTO_DETECT,
       AdapterCapability.BALANCE,
       AdapterCapability.USAGE_STATS,
       AdapterCapability.TOKEN_MANAGEMENT,
       AdapterCapability.MODEL_LIST,
       AdapterCapability.MODEL_PRICING,
      ],
      // 余额/用量字段的单位与换算（rawUnit 每 1 USD 的数量）
      balance: {
        rawUnit: "quota_points",
        conversionFactor: 500000,
      },
   };
   ```

2. **迁移现有 API 调用逻辑**:
   - 从 `services/apiService.ts` 迁移所有 API 调用函数
   - 保持现有的错误处理和重试逻辑
   - 统一余额/用量换算口径：rawUnit=`quota_points`，conversionFactor=500000

3. **实现标准化输出（示例）**:
   ```typescript
   async getAccountBalance(credentials: SiteCredentials): Promise<BalanceInfo> {
     const rawQuota = await this.fetchAccountQuota(credentials);
     return {
       rawBalance: rawQuota,
       rawUnit: "quota_points",
       conversionFactor: 500000,
       balanceUSD: rawQuota / 500000,
     };
   }

   async getUsageStats(credentials: SiteCredentials, timeRange: TimeRange): Promise<UsageStats> {
     const raw = await this.fetchUsage(credentials, timeRange);
     return {
       rawConsumption: raw.today_quota_consumption,
       rawUnit: "quota_points",
       conversionFactor: 500000,
       promptTokens: raw.today_prompt_tokens,
       completionTokens: raw.today_completion_tokens,
       requestCount: raw.today_requests_count,
     };
   }
   ```

4. **处理站点差异**:
   - 充值比例字段差异: `price` vs `stripe_unit_price` vs `PaymentUSDRate`
   - 请求头差异: `New-API-User` vs `Veloera-User`
   - 响应格式差异: 数组 vs 分页对象

#### 2.2 迁移辅助函数
将以下函数从 `apiService.ts` 迁移到 `OneApiAdapter`:
- `createRequestHeaders()` → 私有方法 `createHeaders()`
- `apiRequest()` → 私有方法 `makeRequest()`
- `extractDefaultExchangeRate()` → 私有方法 `extractExchangeRate()`
- `fetchUserInfo()` → 私有方法
- `createAccessToken()` → 私有方法
- 等等...

---

### 阶段 3: 核心服务重构 (AccountManager)

#### 3.1 创建 AccountManager 服务
**文件**: `services/AccountManager.ts`

**核心职责**:
1. 账号的增删改查
2. 调度适配器执行操作
3. 数据格式转换(适配器标准格式 → UI 展示格式)
4. 错误处理和健康状态管理

**核心方法**:
```typescript
export class AccountManager {
  private registry: SiteAdapterRegistry;
  private storage: AccountStorageService;

  // 添加账号
  async addAccount(accountData: AddAccountParams): Promise<string>;

  // 更新账号
  async updateAccount(accountId: string, updateData: UpdateAccountParams): Promise<void>;

  // 删除账号
  async deleteAccount(accountId: string): Promise<void>;

  // 刷新账号数据
  async refreshAccount(accountId: string): Promise<void>;

  // 批量刷新
  async refreshAllAccounts(): Promise<void>;

  // 获取账号列表(转换为展示格式)
  async getDisplayAccounts(): Promise<DisplaySiteData[]>;

  // 自动检测账号
  async autoDetectAccount(siteUrl: string, siteType?: string): Promise<AutoDetectResult>;

  // 验证并保存账号
  async validateAndSaveAccount(params: ValidateAndSaveParams): Promise<AccountSaveResult>;
}
```

**实现要点**:
1. **适配器调度**:
   ```typescript
    async refreshAccount(accountId: string): Promise<void> {
      const account = await this.storage.getAccountById(accountId);
      const adapter = this.registry.getAdapter(account.site_type);

      if (!adapter) {
        throw new Error(`不支持的站点类型: ${account.site_type}`);
      }

      const credentials: SiteCredentials = {
        siteUrl: account.site_url,
        auth: account.account_info.api_key
          ? { kind: "api-key", apiKey: account.account_info.api_key }
          : {
              kind: "one-api-token",
              userId: Number(account.account_info.id),
              accessToken: account.account_info.access_token!,
            },
        adapterConfig: account.adapter_config,
      };

      // 调用适配器获取数据（先检查能力/方法是否存在）
      const [balance, usage] = await Promise.all([
        adapter.getAccountBalance ? adapter.getAccountBalance(credentials) : Promise.resolve(null),
        adapter.getUsageStats ? adapter.getUsageStats(credentials, this.getTodayTimeRange()) : Promise.resolve(null),
      ]);

      // 更新存储
      const nextAccountInfo = { ...account.account_info };
      if (balance) {
        nextAccountInfo.quota = balance.rawBalance;
      }
      if (usage) {
        nextAccountInfo.today_quota_consumption = usage.rawConsumption;
        nextAccountInfo.today_prompt_tokens = usage.promptTokens ?? 0;
        nextAccountInfo.today_completion_tokens = usage.completionTokens ?? 0;
        nextAccountInfo.today_requests_count = usage.requestCount ?? 0;
      }
      await this.storage.updateAccount(accountId, {
        account_info: {
          ...nextAccountInfo,
        },
        last_sync_time: Date.now(),
      });
    }
    ```

2. **数据格式转换**:
   ```typescript
    private convertToDisplayData(account: SiteAccount): DisplaySiteData {
      const adapter = this.registry.getAdapter(account.site_type);
      const conversionFactor = adapter?.metadata.balance?.conversionFactor || 500000;

      return {
        id: account.id,
        icon: account.emoji,
        name: account.site_name,
       username: account.account_info.username,
       balance: {
         USD: parseFloat((account.account_info.quota / conversionFactor).toFixed(2)),
         CNY: parseFloat(((account.account_info.quota / conversionFactor) * account.exchange_rate).toFixed(2)),
       },
       todayConsumption: {
         USD: parseFloat((account.account_info.today_quota_consumption / conversionFactor).toFixed(2)),
         CNY: parseFloat(((account.account_info.today_quota_consumption / conversionFactor) * account.exchange_rate).toFixed(2)),
       },
       // ... 其他字段
     };
   }
   ```

#### 3.2 重构 accountOperations.ts
**文件**: `services/accountOperations.ts`

**修改方案**:
- 保留现有的导出函数(保持向后兼容)
- 内部实现委托给 `AccountManager`
- 逐步迁移调用方到 `AccountManager`

```typescript
// 向后兼容的包装函数
export async function autoDetectAccount(url: string): Promise<AccountValidationResult> {
  const manager = AccountManager.getInstance();
  return manager.autoDetectAccount(url);
}

export async function validateAndSaveAccount(
  url: string,
  siteName: string,
  username: string,
  accessToken: string,
  userId: string,
  exchangeRate: string,
  siteType: string = "one-api"  // 新增参数,默认为 one-api
): Promise<AccountSaveResult> {
  return validateAndSaveAccountV2({
    siteType,
    url,
    siteName,
    username,
    accessToken,
    userId,
    exchangeRate,
  });
}

// 新签名：用参数对象承载不同站点的不同认证字段（推荐 UI 使用）
export async function validateAndSaveAccountV2(
  params: ValidateAndSaveParams
): Promise<AccountSaveResult> {
  const manager = AccountManager.getInstance();
  return manager.validateAndSaveAccount(params);
}
```

---

### 阶段 4: UI 层适配

#### 4.1 更新 AddAccountDialog 组件
**文件**: `components/AddAccountDialog.tsx`

**修改要点**:
1. **添加站点类型选择**:
   ```typescript
   const [siteType, setSiteType] = useState<string>("one-api");
   const supportedSiteTypes = SiteAdapterRegistry.getInstance().getSupportedSiteTypes();
   ```

2. **动态表单字段**:
   - 根据选择的站点类型显示不同认证字段（`access_token/userId` vs `api_key` 等）
   - 根据 `AdapterCapability.AUTO_DETECT` 决定是否展示“自动识别”入口
   - 保存/校验时构造 `ValidateAndSaveParams` 并调用 `validateAndSaveAccountV2(params)`（或直接调用 `AccountManager`）

3. **UI 示例**:
   ```tsx
   const adapter = SiteAdapterRegistry.getInstance().getAdapter(siteType)
   const supportsAutoDetect = adapter?.metadata.capabilities.includes(AdapterCapability.AUTO_DETECT)

   <select value={siteType} onChange={(e) => setSiteType(e.target.value)}>
     {supportedSiteTypes.map(type => (
       <option key={type} value={type}>{type}</option>
     ))}
   </select>

   {/* 根据站点类型显示不同的认证字段 */}
   {siteType === "one-api" && (
     <input placeholder="访问令牌" value={accessToken} onChange={...} />
   )}
   {siteType === "openai" && (
     <input placeholder="API Key" value={apiKey} onChange={...} />
   )}

   {supportsAutoDetect && (
     <button type="button" onClick={handleAutoDetect}>自动识别</button>
   )}
   ```

#### 4.2 更新其他 UI 组件
需要更新的组件:
- `CopyKeyDialog.tsx`: 令牌管理(检查适配器是否支持)
- `ModelList.tsx`: 模型列表(检查适配器是否支持)
- `AccountList.tsx`: 账号列表(可能需要显示站点类型)

**适配器能力检查示例**:
```typescript
const adapter = SiteAdapterRegistry.getInstance().getAdapter(account.site_type);
const supportsTokenManagement = adapter?.metadata.capabilities.includes(
  AdapterCapability.TOKEN_MANAGEMENT
);

if (!supportsTokenManagement) {
  // 隐藏或禁用令牌管理功能
}
```

---

### 阶段 5: 测试和验证

#### 5.1 单元测试
为以下组件编写测试:
- `OneApiAdapter`: 测试所有 API 调用和数据转换
- `SiteAdapterRegistry`: 测试注册、查找、识别
- `AccountManager`: 测试账号管理逻辑

#### 5.2 集成测试
- 测试现有 one-api 账号的迁移
- 测试新增账号的完整流程
- 测试数据刷新和同步

#### 5.3 向后兼容性测试
- 确保现有账号数据能正常加载
- 确保现有功能不受影响
- 测试数据迁移脚本

---

### 阶段 6: 扩展示例 (OpenAI 适配器)

#### 6.1 创建 OpenAiAdapter
**文件**: `adapters/OpenAiAdapter.ts`

**实现要点**:
```typescript
export class OpenAiAdapter implements ISiteAdapter {
  readonly metadata: SiteAdapterMetadata = {
    id: "openai",
    name: "OpenAI 官方 API",
    version: "1.0.0",
    supportedSiteTypes: ["openai"],
    capabilities: [
      AdapterCapability.BALANCE,
      AdapterCapability.USAGE_STATS,
      // 注意: OpenAI 不支持自动检测和令牌管理
    ],
    balance: {
      rawUnit: "USD",
      conversionFactor: 1,
    },
  };

  async validateConnection(credentials: SiteCredentials): Promise<ValidateResult> {
    if (credentials.auth.kind !== "api-key") {
      return { ok: false, message: "OpenAI 适配器需要 api-key 鉴权" };
    }

    // 调用 OpenAI API 验证 API Key
    const response = await fetch("https://api.openai.com/v1/models", {
      headers: {
        Authorization: `Bearer ${credentials.auth.apiKey}`,
      },
    });

    return response.ok
      ? { ok: true }
      : { ok: false, message: `HTTP ${response.status}` };
  }

  async getAccountBalance(credentials: SiteCredentials): Promise<BalanceInfo> {
    if (credentials.auth.kind !== "api-key") {
      throw new Error("OpenAI 适配器需要 api-key 鉴权");
    }

    // 注意：OpenAI 的 billing 相关接口可能会变更/不可用，需要根据实际 API 调整
    const response = await fetch("https://api.openai.com/v1/dashboard/billing/credit_grants", {
      headers: {
        Authorization: `Bearer ${credentials.auth.apiKey}`,
      },
    });
    const data = await response.json();

    return {
      rawBalance: data.total_available,
      rawUnit: "USD",
      conversionFactor: 1,
      balanceUSD: data.total_available,
    };
  }

  async getUsageStats(credentials: SiteCredentials, timeRange: TimeRange): Promise<UsageStats> {
    if (credentials.auth.kind !== "api-key") {
      throw new Error("OpenAI 适配器需要 api-key 鉴权");
    }

    // 调用 OpenAI 用量/账单相关 API（接口可能会变更，此处仅示例）
    // ...
    return {
      rawConsumption: 0,
      rawUnit: "USD",
      conversionFactor: 1,
      promptTokens: 0,
      completionTokens: 0,
      requestCount: 0,
    };
  }
}
```

#### 6.2 注册 OpenAI 适配器
在 `SiteAdapterRegistry` 的 `registerBuiltInAdapters()` 中添加:
```typescript
this.registerAdapter(new OpenAiAdapter());
```

#### 6.3 扩展示例（Cubence 适配器，Cookie 登录态）
Cubence 需要依赖浏览器登录 Cookie 调用用户中心接口（示例接口）：
- `GET https://cubence.com/api/v1/dashboard/overview`
- `GET https://cubence.com/api/v1/analytics/today`

字段映射建议：
- 用户名：`data.user.username`
- 总余额（raw）：`data.balance.total_balance`（单位约等于 `microUSD`）
- 美元余额：`data.balance.total_balance_dollar`（通常等于 `total_balance / 1_000_000`）

适配器元数据建议：
```ts
balance: {
  rawUnit: "micro_usd",
  conversionFactor: 1_000_000
}
```

实现策略：
- `AUTO_DETECT`：通过 overview 读取用户名（不再依赖 localStorage）
- `BALANCE`：读取 `total_balance` 并按 `conversionFactor` 换算展示
- `USAGE_STATS`：通过 analytics/today 获取今日消耗与请求数
  - 今日消耗（raw）：`data.today_consumed_quota`（单位约等于 `microUSD`，与余额同口径）
  - 今日请求数：`data.today_request_count`
  - 今日 tokens：`data.today_tokens`（仅总量；如 UI 需要 upload/download，可先映射到 upload，download 置 0）

---

## 五、关键文件清单

### 新增文件
| 文件路径 | 说明 |
|---------|------|
| `adapters/types.ts` | 适配器公共类型与能力枚举 |
| `adapters/errors.ts` | 适配器错误类型（注册失败/能力不支持） |
| `adapters/ISiteAdapter.ts` | 适配器接口定义 |
| `adapters/SiteAdapterRegistry.ts` | 适配器注册中心 |
| `adapters/OneApiAdapter.ts` | OneAPI 系列适配器 |
| `adapters/CubenceAdapter.ts` | Cubence 适配器（Cookie 登录态） |
| `adapters/OpenAiAdapter.ts` | OpenAI 适配器(示例，可选) |
| `services/AccountManager.ts` | 账号管理核心服务 |

### 修改文件
| 文件路径 | 修改内容 |
|---------|---------|
| `package.json` | 增加 `scripting` 权限（用于 MAIN world fetch） |
| `background.ts` | 新增 `pageFetchJson`（页面上下文请求） |
| `types/index.ts` | 添加 `site_type` 和 `adapter_config` 字段 |
| `services/accountOperations.ts` | 委托实现到 `AccountManager` |
| `services/accountStorage.ts` | 支持新的数据字段 |
| `components/AddAccountDialog.tsx` | 添加站点类型选择 |
| `components/EditAccountDialog.tsx` | 添加站点类型选择与动态字段 |
| `components/CopyKeyDialog.tsx` | 检查适配器能力 |
| `options/pages/ModelList.tsx` | 检查适配器能力 |

### 可选删除文件
| 文件路径 | 说明 |
|---------|------|
| `services/apiService.ts` | 逻辑迁移到 `OneApiAdapter` 后可删除 |

---

## 六、数据迁移策略

### 6.1 现有数据兼容
现有账号数据缺少 `site_type` 字段,需要在加载时自动补充:

```typescript
// 在 AccountStorageService.getAllAccounts() 中（读取后做兼容补齐）
async getAllAccounts(): Promise<SiteAccount[]> {
  const config = await this.getStorageConfig();
  const accounts = config?.accounts || [];

  return accounts.map((account) => ({
    ...account,
    // 自动补充 site_type 字段（默认 one-api）
    site_type: account.site_type ?? "one-api",
    // 适配器配置缺失时补空对象
    adapter_config: account.adapter_config ?? {},
    // 兼容 UI：数值字段缺失时补 0
    account_info: {
      ...account.account_info,
      quota: account.account_info?.quota ?? 0,
      today_prompt_tokens: account.account_info?.today_prompt_tokens ?? 0,
      today_completion_tokens: account.account_info?.today_completion_tokens ?? 0,
      today_quota_consumption: account.account_info?.today_quota_consumption ?? 0,
      today_requests_count: account.account_info?.today_requests_count ?? 0,
    },
  }));
}
```

### 6.2 数据迁移脚本(可选)
如果需要一次性迁移所有数据:

```typescript
async function migrateAccountData() {
  const storage = new AccountStorageService();
  const accounts = await storage.getAllAccounts();

  for (const account of accounts) {
    if (!account.site_type) {
      await storage.updateAccount(account.id, {
        site_type: "one-api",
      });
    }
  }
}
```

---

## 七、实施优先级

### P0 (必须完成)
1. ✅ 创建 `ISiteAdapter` 接口定义
2. ✅ 创建 `SiteAdapterRegistry` 注册中心
3. ✅ 实现 `OneApiAdapter` (迁移现有逻辑)
4. ✅ 创建 `AccountManager` 服务
5. ✅ 更新数据类型定义
6. ✅ 数据迁移兼容处理

### P1 (重要)
7. ✅ 重构 `accountOperations.ts`
8. ✅ 更新 `AddAccountDialog` / `EditAccountDialog` 组件（站点类型选择 + 动态字段）
9. ✅ 更新其他 UI 组件(能力检查)
10. 集成测试和验证

### P2 (可选)
11. 实现 `OpenAiAdapter` 示例
12. 编写单元测试
13. 编写开发文档
14. 性能优化

---

## 八、技术挑战和风险

### 8.1 挑战
1. **数据转换复杂性**: 不同 API 的数据格式差异很大,需要仔细设计转换逻辑
2. **认证方式多样性**: 不同站点的认证方式不同(Bearer Token, API Key, OAuth 等)
3. **功能差异**: 某些站点可能不支持某些功能,需要优雅降级
4. **自动检测限制**: 某些站点可能无法通过 localStorage 自动检测

### 8.2 风险缓解
1. **渐进式迁移**: 先迁移 OneAPI 适配器,确保现有功能不受影响
2. **向后兼容**: 保留现有的导出函数,逐步迁移调用方
3. **充分测试**: 编写完善的测试用例,确保数据迁移正确
4. **文档完善**: 提供清晰的适配器开发文档和示例

---

## 九、后续扩展方向

### 9.1 短期
- 添加更多内置适配器(Claude API, Azure OpenAI 等)
- 支持适配器配置 UI(让用户自定义适配器参数)
- 支持适配器热加载(无需重启插件)

### 9.2 长期
- 支持用户自定义适配器(通过配置文件或脚本)
- 适配器市场(社区贡献的适配器)
- 适配器版本管理和自动更新
- 支持适配器插件系统(中间件、钩子等)

---

## 十、总结

这个设计方案通过**适配器模式**和**注册中心模式**,实现了一个灵活、可扩展的站点类型管理系统。核心优势:

1. **解耦**: 站点特定逻辑与核心业务逻辑分离
2. **扩展性**: 新增站点类型不改核心业务逻辑（新增适配器 + 注册）
3. **向后兼容**: 现有功能和数据无缝迁移
4. **灵活性**: 支持可选功能和自定义配置
5. **可维护性**: 清晰的接口定义和职责划分

实施建议按照 P0 → P1 → P2 的优先级逐步推进,确保每个阶段都经过充分测试和验证。
